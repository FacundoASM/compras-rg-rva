QUÉ INCLUYE ESTE PAQUETE
========================

REEMPLAZA:
  app/acciones.ts               (proveedores, adjuntos, arreglo de costo)
  app/layout.tsx                (pestaña Proveedores en el menú)
  app/globals.css               (formato nuevo de OC + estilos nuevos)
  app/aprobacion/page.tsx       (asignación de proveedor, costos en cualquier etapa)
  app/mis-pedidos/page.tsx      (adjuntos)
  app/dashboard/page.tsx        (proveedor como relación)
  app/oc/[id]/page.tsx          (formato nuevo, una OC por proveedor)

AGREGA:
  app/admin/proveedores/page.tsx
  app/components/SelectorProveedor.tsx
  app/components/Adjuntos.tsx
  supabase/migraciones/02-proveedores-y-adjuntos.sql

BORRAR ESTE ARCHIVO ANTES DE COMMITEAR:
  rm APLICAR.txt

LA BASE DE DATOS YA ESTÁ MIGRADA en el proyecto de Supabase.
El archivo SQL queda solo como documentación para armar un entorno nuevo.

  git add .
  git commit -m "Proveedores, adjuntos y nuevo formato de orden de compra"
  git push
